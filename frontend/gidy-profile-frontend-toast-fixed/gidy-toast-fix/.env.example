# Frontend env example — copy to .env and adjust if needed
# Default: Vite proxy forwards /api → http://localhost:8080
# Only set this if you're NOT using Vite's proxy (e.g. deploying separately)
VITE_API_URL=
